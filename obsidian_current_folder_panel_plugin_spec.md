# Obsidian 插件需求说明与设计说明

# 项目名称

Current Folder Panel（当前目录文件面板）

---

# 一、项目背景

在 Obsidian 的日常使用过程中，用户经常需要频繁查看当前笔记所在目录下的其他文件。

虽然 Obsidian 自带文件树，但存在以下问题：

- 文件树层级过深
- 当前目录定位不够直观
- 频繁展开/折叠目录影响效率
- 当前工作目录的文件缺乏聚焦展示
- 在知识库较大时查找同目录文件效率低

因此，希望开发一个轻量级插件：

> 点击按钮后，在屏幕右侧显示当前文件所在目录下的所有文件。

该插件强调：

- 极简
- 快速
- 无学习成本
- 专注当前工作上下文

---

# 二、项目目标

开发一个 Obsidian 插件，实现：

1. 自动识别当前打开文件所在目录
2. 在右侧侧边栏显示该目录下全部文件
3. 支持快速点击切换文件
4. 当前文件高亮
5. 保持与 Obsidian 原生 UI 风格一致

---

# 三、核心功能需求

## 3.1 当前目录识别

系统应自动识别：

- 当前激活笔记
- 当前笔记所在文件夹

例如：

当前文件：

```text
Projects/TRIZ/Idea.md
```

则插件自动识别目录：

```text
Projects/TRIZ
```

---

## 3.2 右侧目录文件面板

点击插件按钮后：

- 在右侧创建一个 Side Panel
- 展示当前目录下所有文件
- 文件列表自动刷新

显示内容：

- 文件名称
- 当前激活文件高亮
- 文件数量（可选）

---

## 3.3 文件点击打开

点击文件后：

- 在当前 Leaf 中打开
或
- 在新 Leaf 中打开（后续扩展）

---

## 3.4 当前文件高亮

当前正在编辑的文件：

- 背景高亮
- 字体加粗
- 与普通文件区分

---

## 3.5 自动刷新

当用户切换文件时：

插件应自动：

1. 检测当前文件变化
2. 重新获取目录
3. 自动刷新右侧列表

无需手动点击刷新。

---

# 四、MVP 范围

## MVP 包含

- Ribbon 图标
- 当前目录识别
- 右侧面板
- 文件列表展示
- 文件点击打开
- 当前文件高亮
- 自动刷新

---

## MVP 不包含

- 子目录递归
- 拖拽排序
- 标签过滤
- 多目录固定
- 搜索功能
- 最近文件
- 文件预览
- 多窗口同步

---

# 五、用户操作流程

## 操作流程

### 步骤 1

用户打开任意 Markdown 文件。

### 步骤 2

点击左侧 Ribbon 图标。

### 步骤 3

右侧出现：

“当前目录文件列表”。

### 步骤 4

点击列表中的文件。

### 步骤 5

文件打开。

### 步骤 6

切换其他文件时：

目录列表自动更新。

---

# 六、界面设计

# 6.1 Ribbon 图标

位置：

左侧工具栏。

建议图标：

- folder-open
- files
- layout-sidebar-right

---

# 6.2 右侧面板布局

```text
┌──────────────────┐
│ 当前目录：TRIZ   │
├──────────────────┤
│ Idea.md          │
│ Patent.md        │
│ Summary.md       │
│ Research.md      │
└──────────────────┘
```

---

# 6.3 当前文件高亮

例如：

```text
Idea.md   ← 高亮
```

样式：

- 加粗
- 背景色
- hover 效果

---

# 七、技术设计说明

# 7.1 技术架构

插件基于：

- Obsidian Plugin API
- TypeScript
- ESBuild

运行环境：

- Electron
- Obsidian Desktop

---

# 7.2 插件目录结构

```text
obsidian-current-folder-panel/
├── manifest.json
├── main.ts
├── styles.css
├── package.json
└── tsconfig.json
```

---

# 八、核心模块设计

# 8.1 插件主模块

负责：

- 插件注册
- Ribbon 图标注册
- View 注册
- 生命周期管理

核心类：

```ts
export default class CurrentFolderPanelPlugin
```

---

# 8.2 View 模块

负责：

- 创建右侧面板
- 渲染目录文件列表
- 更新 UI

核心类：

```ts
class CurrentFolderPanelView extends ItemView
```

---

# 8.3 文件监听模块

监听：

```ts
workspace.on("active-leaf-change")
```

作用：

当用户切换文件时：

自动刷新目录内容。

---

# 九、核心 API 设计

# 9.1 获取当前文件

```ts
this.app.workspace.getActiveFile()
```

---

# 9.2 获取当前目录

```ts
activeFile.parent
```

---

# 9.3 获取目录下文件

```ts
folder.children
```

---

# 9.4 打开文件

```ts
leaf.openFile(file)
```

---

# 9.5 创建右侧面板

```ts
workspace.getRightLeaf(false)
```

---

# 十、性能设计

由于仅扫描当前目录：

因此性能压力极低。

即使：

- 1000+ 文件库
- 大型 Vault

也不会造成明显性能问题。

原因：

仅处理：

```text
当前目录
```

而非全库索引。

---

# 十一、异常处理

# 11.1 无文件打开

提示：

```text
当前没有打开文件
```

---

# 11.2 根目录情况

若文件位于根目录：

正常显示：

```text
/
```

---

# 11.3 空目录

提示：

```text
当前目录无文件
```

---

# 十二、后续扩展方向

## 可扩展功能

### 1. 子目录树

支持递归展示。

---

### 2. 搜索框

实时搜索当前目录文件。

---

### 3. 排序模式

支持：

- 名称排序
- 修改时间排序
- 创建时间排序

---

### 4. 文件类型过滤

支持：

- Markdown
- PDF
- 图片
- Canvas

---

### 5. 固定目录模式

允许用户固定一个目录长期显示。

---

### 6. 双栏模式

左侧：

目录树

右侧：

文件详情

---

### 7. 最近访问目录

自动记录用户最近目录。

---

# 十三、开发复杂度评估

## 技术复杂度

低。

原因：

- Obsidian API 完整
- 功能边界明确
- 不涉及复杂状态管理
- 不涉及数据库
- 不涉及网络请求

---

## 预计开发时间

| 阶段 | 时间 |
|---|---|
| MVP 开发 | 0.5 ~ 1 天 |
| UI 优化 | 0.5 天 |
| 自动刷新与异常处理 | 0.5 天 |
| 打包测试 | 0.5 天 |

总计：

```text
约 1~2 天
```

---

# 十四、适用场景

适用于：

- 科研知识库
- 项目型 Obsidian 用户
- TRIZ 分析
- 法律文档管理
- 专利资料整理
- 长篇写作
- 多文件协同编辑

尤其适合：

> “围绕当前目录进行工作”的用户。

---

# 十五、最终结论

该插件：

- 实现简单
- 实用性强
- 与 Obsidian 工作流高度契合
- 非常适合作为效率增强插件

建议：

优先开发 MVP 版本。

后续再逐步增加：

- 搜索
- 子目录
- 固定目录
- 文件过滤
- 多面板支持
